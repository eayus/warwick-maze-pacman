public class Wall extends Sprite {
	/*
	 * Very simple class, just used so to identify if a sprite is a wall.
	 * It provides no extra functionality over sprite.
	 */

	public Wall(boolean[][]pixels) {
		super(pixels);
	}

}
